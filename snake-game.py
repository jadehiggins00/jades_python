# this is a snake game
# author: jade higgins
# version: 1.1

import pygame # importing the pygame library
# this intialises all the imported pygame modules, used at the start
pygame.init()
# creating the screen and setting the size
dis = pygame.display.set_model((400, 300))
# the update part is used to update any changes made to the screen
pygame.display.update()
# the quit() is used to unintialise everything at the end of the code
pygame.quit()
quit()
